<div class="create-room">
  <%= link_to "チャットを作成", new_room_path %>
</div>

<div class="rooms">
  <% @rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <%= link_to room.name, room_messages_path(room) %>
      </div>
      <div class="room-info">
        <%= room.info %></a>
      </div>
    </div>
  <% end %>
</div>